{
  "name": "hoquet",
  "description": "Simple templating loosely based on Clojure's Hiccup",
  "version": "0.1.2",
  "main": "index.js",
  "devDependencies": {
    "chai": "*",
    "mocha": "*"
  },
  "scripts": {
    "test": "mocha --no-colors --reporter spec"
  }
}
